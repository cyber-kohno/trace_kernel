# Workspace Generation

- `workspace-version.js`: current `WORKSPACE_GEN` / `RUNTIME_GEN`
- `workspace-fixture.js`: current minimal workspace fixture generator
- `schema/vN.min.json`: saved minimal fixture for each workspace generation

To export the current minimal fixture:

```bash
npm run workspace:fixture
```
